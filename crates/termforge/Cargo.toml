[package]
name = "termforge"
version = "0.1.0"
edition = "2021"
authors = ["Antigravity AGY Team"]
description = "A high-performance, double-buffered terminal game engine in pure Rust"
license = "MIT OR Apache-2.0"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
termion = "3.0"

[dev-dependencies]
criterion = "0.5"
