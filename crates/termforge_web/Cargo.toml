[package]
name = "termforge_web"
version = "0.1.0"
edition = "2021"
authors = ["Antigravity AGY Team"]
description = "WebAssembly bridge for TermForge retro terminal game engine"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
termforge = { path = "../termforge" }
wasm-bindgen = "0.2"
